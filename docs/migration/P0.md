# P0 — Preservación y línea base

Fecha: 2026-09-06. Implementación autorizada por el mensaje del usuario que aprueba MIGRATION_PLAN.md. Estado: línea base técnica establecida; recuperación de Git/procedencia pendiente.

## Componentes modificados

Configuración de empaquetado y pruebas. Las pruebas de contexto ahora usan un corpus sintético no vacío; la integración usa factory_root temporal y comprueba not_answerable cuando faltan fuentes.

## Archivos movidos

Ninguno. Se revisaron imports y referencias del inventario; no se necesita movimiento para esta fase.

## Archivos eliminados

Ninguno. Historial, ERP y cachés originales conservados.

## Archivos creados

- pyproject.toml: runtime estándar Python >=3.11, pytest 9.0.3 ya utilizado por el proyecto, setuptools 80.9.0 como backend de empaquetado.
- requirements-test.lock: dependencias transitivas del entorno de pruebas fijadas por versión; no es un lock de hashes multiplataforma.
- .gitignore: exclusiones raíz de estado, secretos y archivos regenerables.
- docs/migration/baseline-sha256.json: 148 archivos de contenido de la línea base (excluye cachés y directorios administrados por el entorno).
- Este registro y PLAN_ADJUSTMENTS.md.

## Decisiones tomadas

No se inicializa Git ni se inventan fuentes ausentes. Código Python y documentos de diseño copiados a /tmp/nexonova-migration-baseline para comparación; el historial y ERP se preservan in situ con hashes, sin copiarlos a otro destino privado no acordado. No se modifica AGENTS.md ni los tres documentos de auditoría aprobados.

La máquina tiene Python 3.14.4 y Node 22.22.1. pip/ensurepip no estaban disponibles. Se descargó el ejecutable oficial de pip a /tmp y, con autorización de red, se instalaron pytest/setuptools únicamente en /tmp/nexonova-migration-venv. No hay dependencias runtime nuevas.

## Pruebas ejecutadas

- Suite original en copia temporal: 9 passed, 0.11 s.
- Suite con aislamiento/corpus corregidos: 9 passed, 0.06 s.
- Comando: PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /tmp/nexonova-migration-venv/bin/python -B -m pytest -q -p no:cacheprovider.
- python3 -B -m factory.cli --help: exit 0. Imports del núcleo cargan durante pytest.

## Errores encontrados

Git no reconoce esta copia; no hay fuentes de DESIGN_DOCS en el área NexoNova visible. venv sin ensurepip; descargas bloqueadas por DNS del sandbox y completadas fuera de él con autorización. bwrap no puede crear namespaces dentro del sandbox del asistente; su prueba fuera de ese sandbox sí termina con exit 0.

## Problemas pendientes

Confirmar procedencia/derechos y recuperar Git real antes de operaciones de versionado o publicación. Matriz de Python soportada todavía no probada fuera de 3.14.4. La recuperación de fuentes y el traslado de material legado quedan detenidos; continúan correcciones locales que no dependen de ellos.

## Incidencia posterior del 2026-09-07

El reinicio eliminó el baseline y entorno de /tmp. Véase A06: se conserva integridad del material legado por hashes, pero no se dispone de copia del código inicial para reversión exacta. Git/originales deben recuperarse; el checkpoint posterior no sustituye esa copia.
